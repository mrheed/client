export const date = "?requested=" + new Date().getTime()
export const mainURL = "http://localhost:8000"
export const ekstra = mainURL + "/api/ekstra"
export const kelas = mainURL + "/api/kelas" + date
export const auth = mainURL + "/api/auth" + date
export const purge = mainURL + "/api/purge" + date
export const refresh = mainURL + "/api/refresh" + date
export const student = mainURL + "/api/student" + date
export const teacher = mainURL + "/api/teacher"
export const subject = mainURL + "/api/mapel" + date
export const vocational = mainURL + "/api/jurusan" + date
export const competence = mainURL + "/api/kd" + date
export const exam_score = mainURL + "/api/nilai_uh" + date
export const setting = mainURL + "/api/setting"
export const nilai = mainURL + "/api/nilai"
export const misc = mainURL + "/api/misc"
export const cetak = mainURL + "/api/cetak"
